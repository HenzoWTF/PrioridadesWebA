<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">PrioridadesWebA</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="IndexPrioridades">
                <span class="bi bi-database align-items-center d-flex justify-content-center" style="font-size: 1.5em;" aria-hidden="true"></span> Prioridades
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="IndexCliente">
                <span class="bi bi-person-square align-items-center d-flex justify-content-center" style="font-size: 1.5em;" aria-hidden="true"></span> Clientes
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="IndexSistemas">
                <span class="bi bi-device-hdd align-items-center d-flex justify-content-center" style="font-size: 1.5em;" aria-hidden="true"></span> Sistemas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="IndexTicket">
                <span class="bi bi-ticket-fill align-items-center d-flex justify-content-center" style="font-size: 1.5em;" aria-hidden="true" \></span> Tickets
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
